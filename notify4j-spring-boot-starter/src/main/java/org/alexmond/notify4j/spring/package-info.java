/**
 * notify4j Spring Boot starter — auto-configuration that binds {@code notify4j.urls} to channels and
 * wires the {@code Notifications} facade (via a {@code NotificationsFactory}) once the application
 * supplies a {@code NotificationAdapter}. The email channel is optional ({@code spring-boot-starter-mail}).
 *
 * <p>Scaffold placeholder — implementation moved in from {@code builder-notifications-spring-boot-starter}
 * during the extraction.</p>
 */
package org.alexmond.notify4j.spring;
