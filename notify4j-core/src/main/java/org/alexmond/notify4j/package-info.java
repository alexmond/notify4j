/**
 * notify4j core — the Spring-free notification engine: the generic {@code Notifier<E>} SPI, the
 * composition wrappers (composite fan-out, status-transition filtering, runtime mute, reminders), the
 * Apprise-style URL channel parser, and the zero-dependency HTTP channels.
 *
 * <p>Scaffold placeholder — the implementation is moved in here from {@code builder-notifications}
 * during the extraction (see {@code alexmond/builder} docs/design/notify4j-extraction-plan.md).</p>
 */
package org.alexmond.notify4j;
